<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MainActivityContent">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_margin">16dp</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="ItemRowEntry">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:gravity">center|center_horizontal</item>
    </style>

    <style name="CapitalRowItem" parent="ItemRowEntry">
        <item name="android:textAppearance">@style/TextAppearance.AppCompat.Large</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="EditActivityContent">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginLeft">16dp</item>
        <item name="android:layout_marginRight">16dp</item>
    </style>

    <style name="EditActivityTitle" parent="EditActivityContent">
        <item name="android:layout_marginTop">24dp</item>
    </style>

    <style name="EditActivityInput" parent="EditActivityContent">
        <item name="android:layout_marginTop">8dp</item>
        <item name="android:ems">10</item>
        <item name="android:inputType">textShortMessage</item>
    </style>

    <style name="EditActivityInputPrice" parent="EditActivityContent">
        <item name="android:layout_marginTop">8dp</item>
        <item name="android:ems">5</item>
        <item name="android:inputType">numberDecimal</item>
    </style>

</resources>
